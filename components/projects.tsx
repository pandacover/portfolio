import React from "react";

interface Props {
  id: string;
  title: string;
  desc: string;
  image: string;
}

const Projects: React.FC = () => {
  return ( 
      <h1>Projects</h1>
  );
};

export default Projects;
